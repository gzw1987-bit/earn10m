{
  "name": "earn10m",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "sync": "bash scripts/sync-feishu.sh",
    "sync:build": "bash scripts/sync-feishu.sh && next build",
    "deploy": "bash scripts/sync-feishu.sh && git add -A && git commit -m 'update data' && git push && vercel --prod",
    "push": "git add -A && git commit -m 'update' && git push && vercel --prod"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "next": "16.2.3",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.3",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
